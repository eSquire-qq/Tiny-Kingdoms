version: '3.8'
services:
  mysql-db:
    image: mysql:8.0
    container_name: rts-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: rts-game
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: